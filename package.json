{
  "name": "counters2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-ts": "npx tsc",
    "start": "node index.js",
    "deploy": "npm run build-ts && now",
    "start-ts": "ts-node index.ts",
    "dev": "npx nodemon --exec 'npm run start-ts' --watch index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^3.11.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@prisma/client": "^3.11.0",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "lodash": "^4.17.21",
    "p-map": "^2.1.0",
    "typed-assert": "^1.0.9"
  }
}
