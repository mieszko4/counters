type Poll {
  id: ID! @unique
  name: String! @unique
  question: String!
  createdAt: DateTime!
  answers: [Answer!]!
}

type Answer {
  id: ID! @unique
  name: String! @unique
  count: Int! @default(value: 0)
  poll: Poll
}